Drop the final soundtrack here as `theme.mp3`.
The game auto-detects it (src/hooks/useBackgroundMusic.ts) — no code changes needed.
If this file is absent, the game runs fine with just the built-in synthesized SFX.
